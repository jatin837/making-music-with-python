import os
import numpy import array, sin

def main():
    STEP: float = 0.01
    SAMPLING_RATE: float = 48000
    WAVE: np.array = np.linspace(1.0, SAMPLING_RATE, STEP)
     
    

if __name__ = "__main__":
    main()
